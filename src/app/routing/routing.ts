export class Product {
    name: string;
    id: string;
    number: string;
  
    constructor(name: string, id: string, number: string) {
      this.name = name;
      this.id = id;
      this.number = number;
    }
  }